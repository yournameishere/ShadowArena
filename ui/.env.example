# Vite client settings. Copy to ui/.env.local for a live match.
VITE_NETWORK_ID=preprod
VITE_CONTRACT_ADDRESS=
# Optional when deploying/creating a match from the browser; 32-byte coin public key as 64 hex chars.
VITE_OPPONENT_PUBLIC_KEY=
